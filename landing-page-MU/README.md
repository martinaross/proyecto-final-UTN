# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) (or [oxc](https://oxc.rs) when used in [rolldown-vite](https://vite.dev/guide/rolldown)) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
Landing Page Profesional – React (JSX) + CSS

Este proyecto corresponde a una landing page moderna y responsive, desarrollada con React utilizando componentes en JSX y estilos personalizados con CSS mediante un layout organizado y limpio. Está pensada para ser utilizada como sitio de presentación para emprendimientos, marcas personales, servicios o negocios que necesiten una presencia online profesional.

La estructura del proyecto es modular, lo que permite una fácil lectura del código, mantenimiento y futuras ampliaciones. Cada sección se encuentra separada en componentes reutilizables, facilitando la personalización del contenido, colores, imágenes y tipografías según las necesidades del cliente.

La landing cuenta con diseño responsive, adaptándose correctamente a dispositivos móviles, tablets y pantallas de escritorio. El layout CSS fue desarrollado de forma manual, sin frameworks externos, garantizando un diseño liviano, control total sobre los estilos y una carga rápida del sitio.

Tecnologías utilizadas: React, JSX, CSS puro, HTML5 y entorno de desarrollo con Vite (según implementación). El proyecto permite integrar botones de contacto, enlaces externos, redes sociales, imágenes y secciones personalizadas.

Para ejecutar el proyecto de forma local, se debe clonar el repositorio, instalar las dependencias con npm install y ejecutar npm run dev. El sitio se levanta en el navegador en el entorno local configurado por Vite.

El código entregado es completamente editable y preparado para producción, pudiendo desplegarse en plataformas como Netlify, Vercel o GitHub Pages. La landing es ideal para clientes que buscan una solución simple, profesional y escalable, con posibilidad de futuras mejoras o ampliaciones.

Desarrollado por Martina Rossignoli – Front-End Developer. Contacto disponible para personalizaciones, mantenimiento o nuevos desarrollos.